server:
  port: 8083

spring:
  application:
    name: order-service
  data:
    mongodb:
      uri: mongodb://localhost:27017/test_order_db

# Production Service Client Configuration
production:
  service:
    url: http://localhost:8082

management:
  endpoints:
    web:
      exposure:
        include: health



